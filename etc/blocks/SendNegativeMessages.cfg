{
    "conditions": [
        {
            "expr": "{{ $emotions_dict['Anger'] > 0.5 }}"
        },
        {
            "expr": "{{ $emotions_dict['Joy'] < 0.1 }}"
        },
        {
            "expr": "{{ $emotions_dict['Sadness'] > 0.5 }}"
        },
        {
            "expr": "{{ $emotions_dict['Disgust'] > 0.5 }}"
        }
    ],
    "log_level": "INFO",
    "name": "SendNegativeMessages",
    "operator": "ANY",
    "type": "Filter",
    "version": "2.0.0"
}